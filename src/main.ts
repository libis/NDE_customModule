import('./bootstrapCustomModuleCentral').catch((err) => console.error(err));

// // test go back issue
// window.addEventListener('pageshow', (event) => {
//   if (event.persisted) {
//     alert(' Page restored from cache → reload');

//     window.location.reload(); //  force re-init
//   }
// });
